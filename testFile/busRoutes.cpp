#include<iostream>
#include<vector>
#include<bits/stdc++.h>
#include<algorithm>
#include<cmath>
#include<string>
#include<sstream>

using namespace std;

int main()
{
    int t,n,ph=pow(10,9),num;
    /*string check;
    vector<string> te;
    for(int test=0;test<=9;test++){
        te.push_back(test);
    }*/
    vector<int> arr;
    string temp,d;
    vector<string> a;
    string s;
    cin>>t;
    for(int cases=1;cases<=t;cases++){
        cin>>s;
        int len=s.length();
        for(int i=0;i<len;i++){
            cin>>temp;
            a.push_back(temp);
        }
        for(int k=0;k<len;k++){
            cout<<num;
            /*for(int i=1;i<=ph;i++){
                for(int j=1;j<=ph;j++){

                }
            }*/
        }

    }
    //for(int i=1;i<=t;i++)
        //printf("Case #%d: %d\n",i,check[i]);
    return 0;
}
